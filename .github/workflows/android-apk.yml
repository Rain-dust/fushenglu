name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/android-apk.yml"
      - "android/**"
      - "assets/**"
      - "scripts/prepare-capacitor-web.js"
      - "app.js"
      - "capacitor.config.json"
      - "index.html"
      - "manifest.webmanifest"
      - "package-lock.json"
      - "package.json"
      - "styles.css"
      - "sw.js"

permissions:
  contents: write

jobs:
  build-debug-apk:
    name: Build debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK packages
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Install dependencies
        run: npm ci

      - name: Sync Capacitor Android project
        run: npm run cap:sync

      - name: Build APK
        working-directory: android
        run: |
          chmod +x ./gradlew
          ./gradlew assembleDebug --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: fushenglu-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error

      - name: Publish latest debug APK release
        if: github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          cp android/app/build/outputs/apk/debug/app-debug.apk fushenglu-debug.apk
          gh release delete apk-debug-latest --yes --cleanup-tag || true
          gh release create apk-debug-latest fushenglu-debug.apk \
            --target "$GITHUB_SHA" \
            --title "Fushenglu Android APK" \
            --notes "Debug-signed APK built from $GITHUB_SHA. Download fushenglu-debug.apk and install it on Android."
